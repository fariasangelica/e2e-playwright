# e2e-playwright

Projeto de testes end-to-end com Playwright.

## Setup inicial

```bash
npm init playwright@latest --yes -- --quiet --browser=chromium --browser=firefox --browser=webkit --gha
```

Inicializa o projeto Playwright com os 3 browsers (Chromium, Firefox e WebKit) e workflow do GitHub Actions.
